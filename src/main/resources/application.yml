server:
  port: 10330

spring:
  application:
    name: byeol23-front
  profiles:
    active: dev



eureka:
  client:
    healthcheck:
      enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:10338/dashboard/eureka
  instance:
    prefer-ip-address: true
    instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${server.port}}

logging:
  level:
    root: info

minio:
  host: storage.java21.net
  port: 8000
  access-key: V2CnisSrBMa0w2QtE8cP
  secret-key: Bfyx9qcRRI2J7LHF5r5ZoFFWIFSZ40d4K9FEpDnh
  bucket-name: byeol23

aladin:
  base-url: https://www.aladin.co.kr/ttb/api/ItemSearch.aspx
  ttb-key: ${ALADIN_TTB_KEY:ttbrhw25741714001}